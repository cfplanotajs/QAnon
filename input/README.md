# Input Folder

- Place your product PDF at `input/product.pdf`.
- Edit `input/context.txt` with product details before running the pipeline.
- Do **not** commit real product PDFs to version control.
